//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
public class Main {
    public static void main(String[] args) {
        System.out.println("Olá Mundo");
                System.out.println("Me livrei da maldição!");
        int Idade = 29;
        double Altura = 1.62;
        char Gênero = 'M';
        boolean Ativo = true;

        // Tipos primitivos
        // int -> IDADE/NUMEROS INTEIROS
        // double -> NUMERO REAL / QUEBRADOS
        // char -> CARACTER (UMA LETRA OU CARECTERE ESPECIAL)
        // boolean -> VALOR LOGICO ( TRUE OU FALSE) (VERDADEIRO OU FALSE)
        // string -> PALAVRAS / ACEITA MAIS DE UM CARACTERE

        System system = null;
        system.out.println("Idade: " + Idade);
        system.out.println("Altura: " + Altura);
        system.out.println("Gênero: " + Gênero);
        system.out.println("Ativo: " + Ativo);

        // OPERADORES
        // Operadores aritiméticos: +, -, *, /, %

        // Operadores de atribuição: ==, +=, -=, *=, /=, %=

        // Operadores de comparação: ==, !=, >, <, >=, <=

        // ESTRUTURAS DE CONTROLES (IF, ELSE, SWITCH CASE)
        // if = se ( comparação >= ou <=)
        // else = senão
        // Switch case = compara valores e para exemplo numeros com dias da semana




    }
}